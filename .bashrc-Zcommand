#!/hint/bash
#echo -- this is .bashrc-Zcommand -- >2
#echo -- this is .bashrc-Zcommand -- >2
# ~/.bashrc-Zcommand
#

# personal idea 
# because every command uses their command for:
#  grep {-Z, --null}   (for output)
#  grep {-z, --null-data}
#  find -print0
#  xargs {-0, --null}
